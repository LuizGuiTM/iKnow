@model List<CarrinhoViewModel>
<form>
    <table class="table table-striped" style="table-layout:fixed">
        <input type="hidden" id="operacao" value="@ViewBag.Operacao" />
        <tr>
            <th>Prévia</th>
            <th>Nome</th>
            <th>Gênero</th>
            <th>Valor</th>
            <th>Quantidade</th>
        </tr> @foreach (var p in Model)
        {
            <tr>
                <td>
                    <img src="data:image/jpeg;base64,@p.ImagemEmBase64" style="width: 100px; height: auto; object-fit:contain">
                </td>
                <td>@p.Nome</td>
                <td>@p.Categoria</td>
                <td>@p.Preco.ToString("0.00")</td>
                <td>@p.QtdProduto</td>
            </tr>
        }
    </table>
</form>

@if (ViewBag.Erro != null)
{
    <p style="color:red">@ViewBag.Erro</p>
}